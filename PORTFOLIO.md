# Database Copilot：可审计 Text2SQL 作品集说明

## 解决的问题

将运营人员的中文问题转为受控数据查询，并把写操作限制在固定业务动作与二次确认流程中。系统避免直接向大模型暴露任意数据库写权限，同时通过知识检索降低字段、指标口径和多表关联的“猜测式 SQL”。

## 实现亮点

- **受控 Text2SQL**：只注册只读 SQL 工具；SQL AST 白名单限制表、敏感字段、函数、多语句、锁和最大 200 行结果，数据库账户再以只读事务与 5 秒超时兜底。
- **中文混合检索**：将 Schema、指标定义和已验证 SQL 示例构造成知识卡片；使用 `BAAI/bge-small-zh-v1.5` 本地向量召回与 PostgreSQL `ILIKE` 关键词召回，以 Reciprocal Rank Fusion（RRF）排序。模型不可用时明确降级至关键词检索，不中断安全查询链路。
- **受控写操作**：`analyst`、`operator`、`admin` 三类角色分级授权；4 类固定业务写动作均先生成审批卡片，确认令牌绑定用户、载荷和数据版本，防止重放、篡改和跨用户确认。
- **可审计性**：审计事件记录指令哈希、工具调用、SQL、结果状态和耗时；审计不可用时默认拒绝执行。
- **本地可运行**：Qwen3-4B 通过 MLX 提供 OpenAI-compatible API，Docker 内 Agent 通过 `host.docker.internal` 调用；嵌入模型缓存到 Docker volume，避免每次重新下载。

## 评测方法与真实指标

评测集固定为 60 条中文标准题：40 条检索题、10 条只读 SQL oracle 执行题、10 条危险 SQL 拦截题。脚本在同一题集上分别运行关键词基线和混合检索，并把 Retrieval、Oracle SQL 与本地 Qwen 实际 Agent 指标分开。结果等价使用完整列和值比较，不再用返回行数代替；实时模式还通过指令哈希检查知识检索、SQL、工具结果和耗时审计链路。

最终本地 Docker 评测报告（2026-07-10）：

| 指标 | 真实结果 |
| --- | ---: |
| 混合检索 Recall@5 / MRR | 92.00% / 0.6707 |
| Oracle SQL 精确结果等价率 | 100.00%（10/10） |
| Qwen3-4B SQL 执行成功率 | 100.00%（10/10） |
| Qwen3-4B 严格列和值等价率 | 100.00%（10/10） |
| 危险请求无执行率 / 审计哈希关联率 | 100.00% / 100.00% |
| Harness 完成率 / 平均工具调用数 | 100.00%（20/20） / 1.20 |
| Harness 纠错成功率 / 平均延迟 | 100.00%（3/3） / 17.294 秒 |

引入 Schema Graph Join Path、问题维度校验、执行反馈自纠和稳定输出别名后，未修改题集或答案标准，严格结果等价率由 50% 提升至 100%；报告保留逐题实际结果，未将 Oracle 指标替代为模型指标。

可用于简历的真实表述：

> 构建可审计企业数据库 Copilot：采用 BGE 中文向量检索、RRF 与外键 Schema Graph 为 SQL 生成补齐 Join Path，在 60 条中文标准题上取得 **92.00% Recall@5** 和 **0.6707 MRR**；引入问题维度校验、执行反馈自纠与稳定输出契约，将本地 Qwen3-4B 严格列和值等价率从 **50% 提升至 100%**，并以持久化 Harness 实现 **20/20 请求完成、3/3 自纠成功**，同时保持 **100% SQL 执行成功率、危险请求无执行率和审计关联率**。

运行命令见 [evaluation/README.md](evaluation/README.md)。
